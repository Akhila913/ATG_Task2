# Brevo SMTP configuration
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your-verified-brevo-email@example.com
SMTP_PASS=your-brevo-smtp-key
SMTP_SECURE=false

# Email sender address (must match verified Brevo email)
FROM_EMAIL=your-verified-brevo-email@example.com
